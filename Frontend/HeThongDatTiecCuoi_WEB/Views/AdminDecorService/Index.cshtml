@{
    ViewData["Title"] = "Quản Trị Gói Decor & Dịch Vụ";
    Layout = "_AdminLayout";
}

<!-- Nhúng Fonts & Icons chuẩn theo hệ thống thiết kế Opulent Serenade (DESIGN.md) -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,500;0,600;0,700;1,400&family=Plus+Jakarta+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<script src="https://cdn.tailwindcss.com"></script>
<!-- Thêm thư viện SheetJS để xuất Excel thực tế -->
<script src="https://cdn.sheetjs.com/xlsx-0.20.0/package/dist/xlsx.full.min.js"></script>

<script>
    tailwind.config = {
        theme: {
            extend: {
                colors: {
                    brand: {
                        gold: '#B89758',
                        goldLight: '#E8D8B6',
                        goldHover: '#8C6D32',
                        darkNavy: '#111822',
                        sidebar: '#141E28',
                        surface: '#FFFFFF',
                        canvas: '#F8F9FA',
                        border: '#E2E8F0',
                        textMain: '#1E293B',
                        textMuted: '#64748B'
                    }
                },
                fontFamily: {
                    serif: ['"Playfair Display"', 'Georgia', 'serif'],
                    sans: ['"Plus Jakarta Sans"', '-apple-system', 'BlinkMacSystemFont', 'sans-serif']
                }
            }
        }
    }
</script>

<style>
    .decor-page-wrapper {
        font-family: 'Plus Jakarta Sans', sans-serif;
        color: #1E293B;
        background-color: #F8F9FA;
    }

        .decor-page-wrapper .font-serif {
            font-family: 'Playfair Display', serif;
        }

    .custom-card-shadow {
        box-shadow: 0 4px 20px -2px rgba(30, 41, 59, 0.05), 0 2px 6px -1px rgba(30, 41, 59, 0.02);
    }

    .custom-row-hover:hover {
        background-color: #FAF8F5;
        border-color: rgba(184, 151, 88, 0.4);
    }

    .focus-ring:focus-visible {
        outline: 2px solid #B89758;
        outline-offset: 2px;
    }

    .custom-scrollbar::-webkit-scrollbar {
        width: 6px;
    }

    .custom-scrollbar::-webkit-scrollbar-track {
        background: #F7F4EE;
    }

    .custom-scrollbar::-webkit-scrollbar-thumb {
        background: #C5A880;
        border-radius: 4px;
    }

        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
            background: #B89758;
        }
</style>

<!-- CONTAINER CHÍNH: Đã loại bỏ margin âm, thêm w-full để căn chỉnh lề cân đối -->
<div class="decor-page-wrapper min-h-screen flex flex-col w-full relative">

    <!-- ================= KHỐI ĐIỀU HƯỚNG CỐ ĐỊNH (HEADER + TABS DÍNH CHUNG) ================= -->
    <div class="sticky top-0 z-30 bg-white border-b border-slate-200 shadow-xs w-full">

        <!-- Top Title & Controls Header -->
        <header class="px-6 lg:px-8 py-4 flex items-center justify-between border-b border-slate-100 relative">
            <div>
                <!-- Breadcrumbs -->
                <nav aria-label="Breadcrumb" class="flex items-center gap-2 text-xs text-slate-500 mb-1">
                    <span>Quản trị Master Data</span>
                    <i class="fa-solid fa-chevron-right text-[9px] text-slate-400"></i>
                    <span class="text-slate-700 font-medium">Gói Decor &amp; Dịch vụ</span>
                    <i class="fa-solid fa-chevron-right text-[9px] text-slate-400"></i>
                    <span id="breadcrumb-current-tab" class="text-[#B89758] font-semibold">Gói Decor Tiệc Cưới</span>
                </nav>
                <h2 id="header-page-title" class="font-serif text-2xl font-bold text-slate-900 tracking-tight">
                    Quản Trị Danh Mục Gói Decor Tiệc Cưới
                </h2>
            </div>

            <div class="flex items-center gap-3">
                <!-- Live System Indicator -->
                <div class="hidden sm:flex items-center bg-slate-50 rounded-lg px-3 py-1.5 border border-slate-200 text-xs">
                    <span class="w-2 h-2 rounded-full bg-emerald-500 mr-2 animate-pulse"></span>
                    <span id="badge-system-status" class="text-slate-600 font-medium">Hệ thống sẵn sàng • 6 gói cấu hình</span>
                </div>

                <!-- ================= NÚT CHUÔNG & DROPDOWN THÔNG BÁO ================= -->
                <div class="relative" id="notification-wrapper">
                    <button id="btn-notification" onclick="toggleNotificationDropdown()" class="p-2.5 text-slate-500 hover:text-slate-900 bg-slate-50 hover:bg-slate-100 rounded-lg border border-slate-200 relative focus-ring min-h-[42px] min-w-[42px] flex items-center justify-center transition-colors" aria-label="Thông báo hệ thống">
                        <i class="fa-regular fa-bell text-sm"></i>
                        <span id="notif-badge" class="absolute top-2 right-2 w-2 h-2 bg-red-500 rounded-full"></span>
                    </button>

                    <!-- Dropdown Thông Báo -->
                    <div id="notification-dropdown" class="hidden absolute right-0 mt-2 w-80 sm:w-96 bg-white rounded-xl shadow-2xl border border-[#E5D5B8] z-50 overflow-hidden animate-in fade-in zoom-in-95 duration-150">
                        <div class="bg-[#FAF8F5] px-4 py-3 border-b border-slate-200 flex items-center justify-between">
                            <div class="flex items-center gap-2">
                                <i class="fa-solid fa-bell text-[#B89758] text-xs"></i>
                                <span class="font-bold text-xs text-slate-800">Thông báo hệ thống</span>
                            </div>
                            <button onclick="markAllNotificationsRead()" class="text-[11px] text-[#8C6D32] hover:underline font-medium">Đã đọc tất cả</button>
                        </div>
                        <div class="divide-y divide-slate-100 max-h-80 overflow-y-auto custom-scrollbar text-xs">
                            <div class="p-3.5 hover:bg-[#FAF8F5] transition-colors flex gap-3 cursor-pointer">
                                <div class="w-8 h-8 rounded-full bg-emerald-50 border border-emerald-200 text-emerald-600 flex items-center justify-center flex-shrink-0 mt-0.5">
                                    <i class="fa-solid fa-check text-xs"></i>
                                </div>
                                <div class="space-y-0.5 flex-1">
                                    <p class="font-semibold text-slate-800">Gói Decor DC-ROY-07 đã kích hoạt</p>
                                    <p class="text-slate-500 text-[11px] leading-relaxed">Gói "Hoàng Kim Dạ Yến" đã được phê duyệt và niêm yết công khai.</p>
                                    <span class="text-[10px] text-slate-400 block pt-1">5 phút trước</span>
                                </div>
                            </div>
                            <div class="p-3.5 hover:bg-[#FAF8F5] transition-colors flex gap-3 cursor-pointer">
                                <div class="w-8 h-8 rounded-full bg-amber-50 border border-amber-200 text-[#8C6D32] flex items-center justify-center flex-shrink-0 mt-0.5">
                                    <i class="fa-solid fa-pen text-xs"></i>
                                </div>
                                <div class="space-y-0.5 flex-1">
                                    <p class="font-semibold text-slate-800">Cập nhật đơn giá Master Data</p>
                                    <p class="text-slate-500 text-[11px] leading-relaxed">Đơn giá niêm yết của hệ thống Âm thanh JBL đã được cập nhật thành công.</p>
                                    <span class="text-[10px] text-slate-400 block pt-1">1 giờ trước</span>
                                </div>
                            </div>
                            <div class="p-3.5 hover:bg-[#FAF8F5] transition-colors flex gap-3 cursor-pointer">
                                <div class="w-8 h-8 rounded-full bg-blue-50 border border-blue-200 text-blue-600 flex items-center justify-center flex-shrink-0 mt-0.5">
                                    <i class="fa-solid fa-calendar-check text-xs"></i>
                                </div>
                                <div class="space-y-0.5 flex-1">
                                    <p class="font-semibold text-slate-800">Khóa cọc sảnh Seine</p>
                                    <p class="text-slate-500 text-[11px] leading-relaxed">Sảnh Seine (Ca tối, 25/12/2026) đã được cọc Đợt 1 thành công.</p>
                                    <span class="text-[10px] text-slate-400 block pt-1">Hôm nay 09:30</span>
                                </div>
                            </div>
                        </div>
                        <div class="p-2.5 bg-slate-50 border-t border-slate-100 text-center">
                            <button onclick="toggleNotificationDropdown()" class="text-[11px] font-semibold text-slate-600 hover:text-slate-900">Đóng thông báo</button>
                        </div>
                    </div>
                </div>

                <!-- Nút Xuất Excel -->
                <button onclick="exportDataExcel()" class="flex items-center gap-2 px-3.5 py-2 bg-slate-50 hover:bg-slate-100 text-slate-700 rounded-lg text-xs font-semibold transition-colors border border-slate-200 focus-ring min-h-[42px] shadow-2xs">
                    <i class="fa-solid fa-file-excel text-emerald-600 text-sm"></i>
                    <span>Xuất biểu mẫu Excel</span>
                </button>
            </div>
        </header>

        <!-- Two-Tab Catalog Workspace Header -->
        <div class="px-6 lg:px-8 bg-white">
            <div class="flex gap-8" role="tablist">
                <!-- Tab 1 Button -->
                <button id="tab-btn-decor" onclick="switchMainTab('decor')" class="py-4 font-bold text-sm border-b-2 border-[#B89758] text-slate-900 flex items-center gap-2 relative focus-ring transition-colors">
                    <i class="fa-solid fa-wand-magic-sparkles text-[#B89758]"></i>
                    <span>Gói Decor Tiệc Cưới</span>
                    <span id="badge-decor-active" class="bg-[#B89758]/15 text-[#8C6D32] text-[11px] font-bold px-2 py-0.5 rounded-full ml-1 border border-[#B89758]/30">Đang chọn</span>
                </button>

                <!-- Tab 2 Button -->
                <button id="tab-btn-service" onclick="switchMainTab('service')" class="py-4 font-medium text-sm text-slate-500 hover:text-slate-800 flex items-center gap-2 transition-colors border-b-2 border-transparent focus-ring">
                    <i class="fa-solid fa-people-arrows text-slate-400"></i>
                    <span>Dịch Vụ Đi Kèm (Âm thanh, MC, Ban nhạc, LED)</span>
                    <span id="badge-service-count" class="text-[11px] bg-slate-100 text-slate-600 px-2 py-0.5 rounded-full border border-slate-200">6 dịch vụ</span>
                </button>
            </div>
        </div>
    </div>

    <!-- ================= NỘI DUNG VÙNG LÀM VIỆC (WORKSPACE BODY) ================= -->
    <!-- Thêm p-6 và mx-auto để căn giữa nội dung trên màn hình lớn, tạo sự cân đối -->
    <div class="p-6 lg:p-8 space-y-6 flex-1 w-full max-w-[1600px] mx-auto">

        <!-- Master Filter & Action Controls Bar -->
        <section class="bg-white rounded-xl p-4 border border-slate-200 custom-card-shadow flex flex-wrap items-center justify-between gap-4">
            <div class="flex flex-wrap items-center gap-3 flex-1 min-w-[300px]">
                <!-- Search Input -->
                <div class="relative flex-1 min-w-[240px] max-w-sm">
                    <i class="fa-solid fa-magnifying-glass absolute left-3.5 top-1/2 -translate-y-1/2 text-slate-400 text-xs"></i>
                    <input type="text"
                           id="input-search-keyword"
                           oninput="applyFilters()"
                           placeholder="Tìm mã gói, tên chủ đề decor..."
                           class="w-full bg-slate-50 border border-slate-200 rounded-lg pl-9 pr-3 py-2 text-xs text-slate-800 placeholder-slate-400 focus:bg-white focus:outline-none focus:border-[#B89758] focus:ring-1 focus:ring-[#B89758] min-h-[42px] transition-all">
                </div>

                <!-- Category / Style Filter Dropdown -->
                <div class="flex items-center bg-slate-50 border border-slate-200 rounded-lg px-3 py-1 min-h-[42px]">
                    <i id="icon-filter-cat" class="fa-solid fa-palette text-slate-500 mr-2 text-xs"></i>
                    <label for="filter-category" id="label-filter-category" class="text-xs text-slate-500 mr-1.5 whitespace-nowrap">Phong cách:</label>
                    <select id="filter-category" onchange="applyFilters()" class="bg-transparent text-xs font-semibold text-slate-800 focus:outline-none cursor-pointer pr-1">
                        <!-- Options generated dynamically -->
                    </select>
                </div>

                <!-- Status Filter Dropdown -->
                <div class="flex items-center bg-slate-50 border border-slate-200 rounded-lg px-3 py-1 min-h-[42px]">
                    <i class="fa-solid fa-toggle-on text-slate-500 mr-2 text-xs"></i>
                    <label for="filter-status" class="text-xs text-slate-500 mr-1.5 whitespace-nowrap">Trạng thái:</label>
                    <select id="filter-status" onchange="applyFilters()" class="bg-transparent text-xs font-semibold text-slate-800 focus:outline-none cursor-pointer pr-1">
                        <option value="">Tất cả trạng thái</option>
                        <option value="Áp dụng">Áp dụng</option>
                        <option value="Ngừng áp dụng">Ngừng áp dụng</option>
                    </select>
                </div>

                <!-- Reset Filter Action -->
                <button onclick="resetFilters()" class="px-3.5 py-2 bg-slate-100 hover:bg-slate-200 text-slate-700 rounded-lg text-xs font-semibold transition-colors border border-slate-200 flex items-center gap-1.5 focus-ring min-h-[42px]">
                    <i class="fa-solid fa-arrow-rotate-left text-[11px] text-slate-500"></i>
                    <span>Đặt lại</span>
                </button>
            </div>

            <!-- Primary Action CTA Button -->
            <button onclick="openCreateModal()" class="px-5 py-2.5 bg-gradient-to-r from-[#B89758] to-[#9E7E3F] hover:from-[#A8884A] hover:to-[#8E7035] text-white font-bold text-xs rounded-lg shadow-sm flex items-center gap-2 transition-all focus-ring min-h-[42px] flex-shrink-0 cursor-pointer">
                <i class="fa-solid fa-plus text-xs"></i>
                <span id="btn-create-label">Thêm gói Decor</span>
            </button>
        </section>

        <!-- Status Summary Metrics Bar -->
        <div class="flex items-center justify-between text-xs text-slate-500 px-1">
            <div class="flex items-center gap-3">
                <span id="metric-title-label" class="font-medium text-slate-700">Danh mục Gói Decor đang quản lý:</span>
                <span id="metric-showing" class="px-2.5 py-0.5 rounded-full bg-slate-200/80 font-bold text-slate-800 text-[11px]">Hiển thị 6 gói</span>
            </div>
            <div class="flex items-center gap-4 text-[11px]">
                <span id="metric-active" class="flex items-center gap-1.5 text-emerald-700 font-medium">
                    <span class="w-2 h-2 rounded-full bg-emerald-500"></span> 5 gói Đang áp dụng
                </span>
                <span id="metric-inactive" class="flex items-center gap-1.5 text-slate-500 font-medium">
                    <span class="w-2 h-2 rounded-full bg-slate-400"></span> 1 gói Ngừng áp dụng
                </span>
            </div>
        </div>

        <!-- VIEW CONTAINER 1: TAB 1 (GÓI DECOR - LƯỚI CARD 3 CỘT) -->
        <div id="container-decor-tab" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <!-- Cards rendered dynamically by JS -->
        </div>

        <!-- VIEW CONTAINER 2: TAB 2 (DỊCH VỤ - BẢNG VẬN HÀNH) -->
        <div id="container-service-tab" class="bg-white rounded-xl border border-slate-200 custom-card-shadow overflow-hidden hidden">
            <div class="overflow-x-auto">
                <table class="w-full border-collapse text-left">
                    <thead>
                        <tr class="bg-[#FAF8F5] text-slate-700 border-b border-slate-200 text-xs font-semibold uppercase tracking-wider">
                            <th class="py-3.5 px-4 w-28 text-center">Mã DV</th>
                            <th class="py-3.5 px-4 w-40">Hình ảnh</th>
                            <th class="py-3.5 px-5">Tên Dịch Vụ &amp; Mô Tả Chi Tiết</th>
                            <th class="py-3.5 px-4 w-48">Phân Loại Dịch Vụ</th>
                            <th class="py-3.5 px-5 w-44 text-right">Đơn Giá Niêm Yết</th>
                            <th class="py-3.5 px-4 w-36 text-center">Trạng Thái</th>
                            <th class="py-3.5 px-4 w-44 text-center">Thao Tác</th>
                        </tr>
                    </thead>
                    <tbody id="service-table-tbody" class="divide-y divide-slate-100 text-xs">
                        <!-- Rows rendered dynamically by JS -->
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Pagination / Summary Bar -->
        <div class="bg-white rounded-xl p-4 border border-slate-200 custom-card-shadow flex flex-wrap items-center justify-between gap-4 text-xs text-slate-600">
            <div class="flex items-center gap-2">
                <span id="pagination-summary">Hiển thị <strong>1 – 6</strong> trong tổng số <strong>6</strong> gói Decor</span>
                <span class="text-slate-300">|</span>
                <span class="text-slate-500">Quy chuẩn dữ liệu: Master Data Spec MD-RP-2026</span>
            </div>
            <div class="flex items-center gap-2">
                <button class="px-3.5 py-1.5 rounded-lg border border-slate-200 bg-slate-50 hover:bg-slate-100 text-slate-600 font-medium focus-ring disabled:opacity-50" disabled>
                    <i class="fa-solid fa-chevron-left mr-1 text-[10px]"></i> Trang trước
                </button>
                <span class="px-3 py-1.5 rounded-lg bg-[#B89758]/15 text-[#8C6D32] font-bold border border-[#B89758]/30">1</span>
                <button class="px-3.5 py-1.5 rounded-lg border border-slate-200 bg-slate-50 hover:bg-slate-100 text-slate-600 font-medium focus-ring disabled:opacity-50" disabled>
                    Trang sau <i class="fa-solid fa-chevron-right ml-1 text-[10px]"></i>
                </button>
            </div>
        </div>

        <!-- Master Data Guidelines & Technical Policy Banner -->
        <div class="p-4 rounded-xl bg-amber-50/50 border border-[#B89758]/30 flex items-start gap-3 text-xs">
            <div class="w-6 h-6 rounded-full bg-[#B89758]/20 flex items-center justify-center text-[#967638] flex-shrink-0 mt-0.5">
                <i class="fa-solid fa-circle-info text-xs"></i>
            </div>
            <div class="space-y-1">
                <h5 class="font-bold text-[#1E242B]">Quy định cấu hình Danh mục Master Data Riverside Palace:</h5>
                <p class="text-slate-600 leading-relaxed">
                    Theo quy chuẩn vận hành, hệ thống không cho phép xóa vĩnh viễn dữ liệu gói decor và dịch vụ đã từng phát sinh giao dịch trên các hợp đồng tiệc cưới. Quản trị viên sử dụng thao tác <strong>Khóa (Tạm ngưng)</strong> để bảo toàn toàn vẹn dữ liệu kế toán và lịch sử kiểm toán hợp đồng.
                </p>
            </div>
        </div>

    </div>

    <!-- Admin Footer -->
    <footer class="mt-auto bg-white border-t border-slate-200 px-6 lg:px-8 py-3.5 flex flex-wrap items-center justify-between text-xs text-slate-500 gap-2">
        <div>
            © 2026 Riverside Palace Convention &amp; Wedding Center. Quản Trị Hệ Thống Master Data v4.2.
        </div>
        <div class="flex items-center gap-4">
            <span class="flex items-center gap-1 text-slate-600">
                <i class="fa-solid fa-shield-halved text-[#B89758]"></i> Phân quyền: Administrator Level 1
            </span>
            <span>•</span>
            <span>Hotline Kỹ thuật &amp; IT nội bộ: Ext 888</span>
        </div>
    </footer>
</div>

<!-- ========================================================================= -->
<!-- ================= MODAL CHÍNH: THÊM GÓI DECOR MỚI ======================== -->
<!-- ========================================================================= -->
<div id="decorModal" class="fixed inset-0 bg-black/60 backdrop-blur-xs flex items-center justify-center z-50 hidden p-4 sm:p-6 overflow-y-auto">
    <div class="bg-white border border-[#E5D5B8] rounded-2xl w-full max-w-4xl shadow-2xl flex flex-col max-h-[92vh] overflow-hidden my-auto animate-in fade-in zoom-in-95 duration-200">

        <!-- Modal Header -->
        <div class="px-8 py-5 border-b border-slate-200 bg-[#FAF8F5] flex items-center justify-between">
            <div class="flex items-center space-x-3">
                <div class="w-10 h-10 rounded-full bg-[#B89758]/15 border border-[#E5D5B8] flex items-center justify-center text-[#B89758] flex-shrink-0">
                    <i class="fa-solid fa-wand-magic-sparkles text-sm"></i>
                </div>
                <div>
                    <div class="flex items-center space-x-2.5">
                        <h2 id="decor-modal-title" class="font-serif text-xl font-bold text-slate-900">Thêm gói Decor mới</h2>
                        <span class="px-2 py-0.5 rounded-full bg-[#B89758]/15 border border-[#B89758]/30 text-[10px] font-bold text-[#8C6D32] tracking-wider uppercase">MASTER DATA</span>
                    </div>
                    <p class="text-xs text-slate-500 mt-0.5">Cấu hình thông tin danh mục, định mức giá và hình ảnh nhận diện gói trang trí tiệc cưới.</p>
                </div>
            </div>
            <button onclick="closeDecorModal()" class="w-9 h-9 rounded-full flex items-center justify-center text-slate-400 hover:text-slate-800 hover:bg-slate-200 transition-colors focus-ring" type="button" aria-label="Đóng">
                <i class="fa-solid fa-xmark text-base"></i>
            </button>
        </div>

        <!-- Modal Body (Form Layout with Two Main Logical Sections) -->
        <form id="decorForm" onsubmit="handleDecorFormSubmit(event)" class="p-8 overflow-y-auto custom-scrollbar space-y-6 bg-[#FDFBF7] text-xs">
            <input type="hidden" id="decor-form-id" value="0" />

            <!-- PHẦN 1: THÔNG TIN CƠ BẢN & ĐỊNH MỨC KINH DOANH -->
            <div class="bg-white rounded-xl p-6 border border-[#E5D5B8] shadow-xs space-y-5">
                <div class="flex items-center justify-between border-b border-slate-200 pb-3">
                    <div class="flex items-center space-x-2">
                        <i class="fa-solid fa-sliders text-[#B89758] text-sm"></i>
                        <h3 class="font-serif text-base font-bold text-slate-900">1. Thông tin cơ bản &amp; Định mức kinh doanh</h3>
                    </div>
                    <span class="text-[10px] text-[#C5A880] font-bold tracking-wider uppercase">PHẦN THIẾT YẾU</span>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-12 gap-4">
                    <!-- Mã gói decor (Tự động) -->
                    <div class="md:col-span-3">
                        <label class="block font-semibold text-slate-700 mb-1" for="decor-code">
                            Mã gói decor <span class="text-slate-400 font-normal">(Tự động)</span>
                        </label>
                        <div class="relative">
                            <input id="decor-code" class="w-full bg-slate-100 border border-slate-200 text-slate-700 font-mono font-bold rounded-lg px-3 py-2 text-xs cursor-not-allowed select-all focus:outline-none" readonly type="text" value="DC-ROY-07" />
                            <i class="fa-solid fa-lock absolute right-3 top-2.5 text-slate-400 text-xs"></i>
                        </div>
                    </div>

                    <!-- Tên gói Decor* -->
                    <div class="md:col-span-5">
                        <label class="block font-semibold text-slate-800 mb-1" for="decor-name">
                            Tên gói Decor <span class="text-red-500">*</span>
                        </label>
                        <input id="decor-name" required class="w-full bg-[#FAF8F5] border border-slate-200 focus:border-[#B89758] focus:bg-white focus:ring-1 focus:ring-[#B89758] rounded-lg px-3 py-2 text-xs text-slate-800 font-medium outline-none transition-all" placeholder="Nhập tên gói sang trọng..." type="text" value="Hoàng Kim Dạ Yến" />
                    </div>

                    <!-- Phong cách decor* Dropdown -->
                    <div class="md:col-span-4">
                        <label class="block font-semibold text-slate-800 mb-1" for="decor-style">
                            Phong cách decor <span class="text-red-500">*</span>
                        </label>
                        <select id="decor-style" class="w-full bg-[#FAF8F5] border border-slate-200 focus:border-[#B89758] focus:bg-white focus:ring-1 focus:ring-[#B89758] rounded-lg px-3 py-2 text-xs text-slate-800 outline-none cursor-pointer">
                            <option selected>Hoàng gia (Royal Luxury)</option>
                            <option>Tối giản &amp; Hiện đại (Minimalist)</option>
                            <option>Cổ điển (Vintage Romance)</option>
                            <option>Dạ khúc Ánh sao (Celestial)</option>
                            <option>Sang trọng bờ sông (Riverside Luxe)</option>
                            <option>Venice Lãng mạn</option>
                        </select>
                    </div>

                    <!-- Hạng mục chính bao gồm (5 Checkboxes) -->
                    <div class="md:col-span-12">
                        <label class="block font-semibold text-slate-800 mb-2">
                            Hạng mục chính đi kèm <span class="text-slate-500 font-normal">(Được tích hợp mặc định trong gói)</span>
                        </label>
                        <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-5 gap-2.5">
                            <label class="flex items-center space-x-2 p-2 rounded-lg border border-slate-200 bg-[#FAF8F5] hover:border-[#B89758] cursor-pointer transition-colors">
                                <input id="chk-backdrop" type="checkbox" checked class="w-3.5 h-3.5 text-[#B89758] focus:ring-[#B89758] rounded border-slate-300">
                                <span class="text-xs text-slate-700 font-medium">Backdrop sân khấu</span>
                            </label>
                            <label class="flex items-center space-x-2 p-2 rounded-lg border border-slate-200 bg-[#FAF8F5] hover:border-[#B89758] cursor-pointer transition-colors">
                                <input id="chk-loidi" type="checkbox" checked class="w-3.5 h-3.5 text-[#B89758] focus:ring-[#B89758] rounded border-slate-300">
                                <span class="text-xs text-slate-700 font-medium">Lối đi hoa tươi</span>
                            </label>
                            <label class="flex items-center space-x-2 p-2 rounded-lg border border-slate-200 bg-[#FAF8F5] hover:border-[#B89758] cursor-pointer transition-colors">
                                <input id="chk-gallery" type="checkbox" checked class="w-3.5 h-3.5 text-[#B89758] focus:ring-[#B89758] rounded border-slate-300">
                                <span class="text-xs text-slate-700 font-medium">Bàn gallery đón khách</span>
                            </label>
                            <label class="flex items-center space-x-2 p-2 rounded-lg border border-slate-200 bg-[#FAF8F5] hover:border-[#B89758] cursor-pointer transition-colors">
                                <input id="chk-conghoa" type="checkbox" checked class="w-3.5 h-3.5 text-[#B89758] focus:ring-[#B89758] rounded border-slate-300">
                                <span class="text-xs text-slate-700 font-medium">Cổng hoa vòm LED</span>
                            </label>
                            <label class="flex items-center space-x-2 p-2 rounded-lg border border-slate-200 bg-[#FAF8F5] hover:border-[#B89758] cursor-pointer transition-colors">
                                <input id="chk-phale" type="checkbox" checked class="w-3.5 h-3.5 text-[#B89758] focus:ring-[#B89758] rounded border-slate-300">
                                <span class="text-xs text-slate-700 font-medium">Đèn chùm pha lê</span>
                            </label>
                        </div>
                    </div>

                    <!-- Thời gian set-up dự kiến -->
                    <div class="md:col-span-4">
                        <label class="block font-semibold text-slate-800 mb-1" for="decor-setup-time">
                            Thời gian set-up dự kiến
                        </label>
                        <div class="relative">
                            <i class="fa-regular fa-clock absolute left-3 top-2.5 text-slate-400 text-xs"></i>
                            <input id="decor-setup-time" class="w-full bg-[#FAF8F5] border border-slate-200 focus:border-[#B89758] focus:bg-white rounded-lg pl-8 pr-3 py-2 text-xs text-slate-800 outline-none" type="text" value="4.0 giờ trước ca tiệc" />
                        </div>
                    </div>

                    <!-- Đơn giá niêm yết (VNĐ)* -->
                    <div class="md:col-span-4">
                        <label class="block font-semibold text-slate-800 mb-1" for="decor-price">
                            Đơn giá niêm yết (VNĐ)*
                        </label>
                        <div class="relative">
                            <input id="decor-price" required class="w-full bg-[#FAF8F5] border border-slate-200 focus:border-[#B89758] focus:bg-white rounded-lg pl-3 pr-16 py-2 text-xs font-bold text-[#8C6D32] outline-none" type="number" min="0" step="500000" value="72000000" />
                            <span class="absolute right-3 top-2 text-[11px] font-bold text-slate-500">VNĐ / tiệc</span>
                        </div>
                    </div>

                    <!-- Trạng thái áp dụng -->
                    <div class="md:col-span-4">
                        <label class="block font-semibold text-slate-800 mb-1">
                            Trạng thái áp dụng
                        </label>
                        <div class="flex items-center space-x-3 pt-1">
                            <label class="flex items-center space-x-1.5 cursor-pointer">
                                <input id="radio-decor-active" checked name="decor_status" type="radio" value="Áp dụng" class="w-3.5 h-3.5 text-[#B89758] focus:ring-[#B89758] border-slate-300">
                                <span class="px-2 py-0.5 rounded-full bg-emerald-50 text-emerald-800 border border-emerald-200 text-[11px] font-semibold flex items-center space-x-1">
                                    <span class="w-1.5 h-1.5 rounded-full bg-emerald-600"></span>
                                    <span>Áp dụng ngay</span>
                                </span>
                            </label>
                            <label class="flex items-center space-x-1.5 cursor-pointer">
                                <input id="radio-decor-draft" name="decor_status" type="radio" value="Ngừng áp dụng" class="w-3.5 h-3.5 text-[#B89758] focus:ring-[#B89758] border-slate-300">
                                <span class="px-2 py-0.5 rounded-full bg-slate-100 text-slate-600 border border-slate-200 text-[11px] font-medium">Bản nháp lưu trữ</span>
                            </label>
                        </div>
                    </div>

                    <!-- Mô tả chi tiết concept & Quy chuẩn vật liệu -->
                    <div class="md:col-span-12">
                        <label class="block font-semibold text-slate-800 mb-1" for="decor-desc">
                            Mô tả chi tiết concept &amp; Quy chuẩn vật liệu
                        </label>
                        <textarea id="decor-desc" rows="2" class="w-full bg-[#FAF8F5] border border-slate-200 focus:border-[#B89758] focus:bg-white rounded-lg p-3 text-xs text-slate-800 leading-relaxed outline-none" placeholder="Tóm tắt phong cách thiết kế, sắc độ hoa chủ đạo, hệ đèn chiếu...">Gói trang trí Hoàng Kim Dạ Yến sử dụng gam màu vàng Champagne kết hợp trắng ngà thanh thoát, hoa lan hồ điệp tươi kết hợp hoa hồng nhập khẩu. Điểm nhấn là vòm cổng pha lê cao cấp và hệ đèn chandelier lộng lẫy kiến tạo không gian đại sảnh đẳng cấp quý tộc.</textarea>
                    </div>
                </div>
            </div>

            <!-- PHẦN 2: HÌNH ẢNH ĐẠI DIỆN (COVER PHOTO) -->
            <div class="bg-white rounded-xl p-6 border border-[#E5D5B8] shadow-xs space-y-4">
                <div class="flex items-center justify-between border-b border-slate-200 pb-3">
                    <div>
                        <div class="flex items-center space-x-2">
                            <i class="fa-regular fa-image text-[#B89758] text-sm"></i>
                            <h3 class="font-serif text-base font-bold text-slate-900">2. Hình ảnh đại diện (Cover Photo)</h3>
                        </div>
                        <p class="text-xs text-slate-500 mt-0.5">Hình ảnh thực tế chuẩn độ phân giải cao phục vụ hợp đồng và tư vấn khách hàng.</p>
                    </div>
                    <span class="px-2.5 py-0.5 rounded-full bg-[#B89758]/15 border border-[#B89758]/30 text-[11px] font-semibold text-[#8C6D32]">Quy chuẩn 16:9</span>
                </div>

                <!-- Dropzone Kéo Thả Ảnh -->
                <div id="decor-dropzone" onclick="document.getElementById('decor-file-input').click()" class="border-2 border-dashed border-[#E5D5B8] hover:border-[#B89758] rounded-xl p-6 text-center bg-[#FAF8F5]/80 hover:bg-white transition-all cursor-pointer flex flex-col items-center justify-center">
                    <input type="file" id="decor-file-input" accept="image/*" class="hidden" onchange="handleImageSelected(event)" />
                    <div class="w-12 h-12 rounded-full bg-white border border-[#E5D5B8] flex items-center justify-center text-[#B89758] shadow-xs mb-2">
                        <i class="fa-solid fa-cloud-arrow-up text-lg"></i>
                    </div>
                    <p class="font-serif text-sm font-semibold text-slate-800 mb-1">Kéo thả ảnh vào đây hoặc chọn từ máy</p>
                    <p class="text-[11px] text-slate-400">Định dạng hỗ trợ: JPG, JPEG, PNG, WEBP • Dung lượng tối đa: dưới 5 MB</p>
                </div>

                <!-- Card Xem Trước Ảnh (Preview Card) -->
                <div id="decor-image-preview-card" class="bg-[#FAF8F5] rounded-xl border border-[#E5D5B8] p-3 flex flex-col sm:flex-row items-center justify-between gap-3 shadow-2xs">
                    <div class="flex items-center space-x-3 w-full sm:w-auto">
                        <div class="relative w-24 h-16 rounded-lg overflow-hidden border border-slate-200 flex-shrink-0 bg-slate-900 shadow-inner">
                            <img id="decor-preview-img" src="https://lh3.googleusercontent.com/aida/AEtjO1UlG53VliMfYaOPGWYTFo5zD5pEzc698JZZ3xtxh3ZJzk9TH4CKxNGW6GRCm35bJ5pSfaMfASYwgstAXnJfCb4pb8Ri73BetKzuyD9A1ZBVRo0tyUm1tOg7LoWxE_EwngYzmPNjYi7IYvP3Zxc0rUiuzJwCq8CIR3R327AKzJ6fz-C8v6WzJjdkOdsvUKVODnM21hg2IkUq4BQYQeXrPz_QG7aMOPFALohu5Lp2kFCu2PsDU5TivtSPHqU" class="w-full h-full object-cover" alt="Preview" />
                            <span class="absolute bottom-1 right-1 bg-black/70 text-white text-[8px] font-mono px-1 rounded">16:9</span>
                        </div>
                        <div class="space-y-0.5">
                            <div class="flex items-center space-x-2">
                                <p id="decor-preview-filename" class="font-semibold text-xs text-slate-800 truncate max-w-xs">hoang-kim-da-yen-cover-4k.jpg</p>
                                <span class="px-2 py-0.5 rounded-full bg-emerald-100 text-emerald-800 text-[10px] font-semibold flex items-center space-x-1">
                                    <i class="fa-solid fa-circle-check text-[10px]"></i>
                                    <span>Hợp lệ</span>
                                </span>
                            </div>
                            <p class="text-[11px] text-slate-500">Kích thước: 1920 × 1080 px • Dung lượng: 3.4 MB • sRGB</p>
                        </div>
                    </div>
                    <div class="flex items-center space-x-2 w-full sm:w-auto justify-end">
                        <button type="button" onclick="document.getElementById('decor-file-input').click()" class="h-9 px-3 rounded-lg border border-[#E5D5B8] bg-white hover:bg-[#FAF8F5] text-slate-700 text-xs font-semibold flex items-center space-x-1.5 transition-colors">
                            <i class="fa-solid fa-arrows-rotate text-[11px] text-[#B89758]"></i>
                            <span>Thay đổi</span>
                        </button>
                        <button type="button" onclick="removeImagePreview()" class="h-9 px-2.5 rounded-lg border border-red-200 text-red-600 hover:bg-red-50 text-xs transition-colors" title="Gỡ bỏ ảnh">
                            <i class="fa-solid fa-trash-can text-xs"></i>
                        </button>
                    </div>
                </div>
            </div>

            <!-- Modal Footer Controls -->
            <div class="pt-3 border-t border-slate-200 flex flex-col sm:flex-row items-center justify-between gap-4">
                <p class="text-[11px] text-[#8C6D32] italic">
                    * Các trường đánh dấu sao (<span class="text-red-500 font-bold">*</span>) bắt buộc phải nhập đầy đủ để kích hoạt gói vào danh mục công khai.
                </p>
                <div class="flex items-center space-x-2.5 w-full sm:w-auto justify-end">
                    <button type="button" onclick="closeDecorModal()" class="h-10 px-5 rounded-lg border border-slate-300 bg-white hover:bg-slate-100 text-slate-700 font-semibold transition-colors focus-ring">
                        Hủy bỏ
                    </button>
                    <button type="submit" class="h-10 px-6 rounded-lg bg-gradient-to-r from-[#B89758] to-[#9E7E3F] hover:from-[#A8884A] hover:to-[#8E7035] text-white font-bold tracking-wide shadow-sm flex items-center space-x-2 transition-all focus-ring">
                        <i class="fa-solid fa-check text-xs"></i>
                        <span>LƯU GÓI DECOR</span>
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>

<!-- ================= MODAL CHI TIẾT GÓI DECOR / DỊCH VỤ ================= -->
<div id="detailModal" class="fixed inset-0 bg-slate-950/60 backdrop-blur-xs flex items-center justify-center z-50 hidden p-4">
    <div class="bg-white rounded-2xl w-full max-w-lg border border-[#E5D5B8] shadow-2xl overflow-hidden animate-in fade-in zoom-in-95 duration-200">
        <div class="bg-[#FAF8F5] px-6 py-4 border-b border-slate-200 flex items-center justify-between">
            <h3 class="font-serif font-bold text-lg text-slate-900 flex items-center gap-2">
                <i class="fa-solid fa-crown text-[#B89758] text-sm"></i>
                <span id="detail-title">Chi Tiết Hạng Mục</span>
            </h3>
            <button onclick="closeDetailModal()" class="text-slate-400 hover:text-slate-700 p-1.5 rounded-lg hover:bg-slate-200 transition-colors">
                <i class="fa-solid fa-xmark text-base"></i>
            </button>
        </div>
        <div id="detail-content" class="p-6 space-y-4 text-xs">
            <!-- Rendered by JS -->
        </div>
        <div class="bg-slate-50 px-6 py-3 border-t border-slate-100 text-right">
            <button onclick="closeDetailModal()" class="px-4 py-2 bg-slate-200 hover:bg-slate-300 text-slate-800 rounded-lg font-semibold transition-colors">Đóng Cửa Sổ</button>
        </div>
    </div>
</div>

<!-- ================= JAVASCRIPT XỬ LÝ NGHIỆP VỤ & TƯƠNG TÁC ================= -->
@section Scripts {
    <script>
        // 1. DỮ LIỆU ĐỒNG BỘ 100% CSDL SQL VÀ MẪU
        let currentTab = 'decor'; // 'decor' hoặc 'service'

        let decorData = [
            {
                id: 1,
                ma: 'DC-ROYAL-01',
                ten: 'Hoàng Kim Mộng Điệp',
                phanLoai: 'Hoàng gia (Royal Luxury)',
                gia: 68000000,
                moTa: 'Backdrop vòm cung dát vàng ánh kim, kết hợp 100% hoa lan hồ điệp trắng nhập khẩu, đèn chùm pha lê và nến lung linh dọc lối đi lễ đường.',
                spec1: 'Sân khấu • Lối đi • Bàn VIP',
                spec2: '4 giờ trước ca tiệc',
                hinhAnh: 'https://lh3.googleusercontent.com/aida/AEtjO1UlG53VliMfYaOPGWYTFo5zD5pEzc698JZZ3xtxh3ZJzk9TH4CKxNGW6GRCm35bJ5pSfaMfASYwgstAXnJfCb4pb8Ri73BetKzuyD9A1ZBVRo0tyUm1tOg7LoWxE_EwngYzmPNjYi7IYvP3Zxc0rUiuzJwCq8CIR3R327AKzJ6fz-C8v6WzJjdkOdsvUKVODnM21hg2IkUq4BQYQeXrPz_QG7aMOPFALohu5Lp2kFCu2PsDU5TivtSPHqU',
                trangThai: 'Áp dụng'
            },
            {
                id: 2,
                ma: 'DC-MOD-02',
                ten: 'Ngọc Lục Thanh Khiết',
                phanLoai: 'Tối giản & Hiện đại (Minimalist)',
                gia: 45000000,
                moTa: 'Sự kết hợp tinh tế giữa vòm hoa lá thảo mộc tự nhiên, hoa tú cầu trắng và hệ thống nến thủy tinh ấm áp mang tinh thần thiên nhiên xanh mát.',
                spec1: 'Cổng hoa vòm • 10 trụ lối đi',
                spec2: '3.5 giờ trước ca tiệc',
                hinhAnh: 'https://lh3.googleusercontent.com/aida/AEtjO1XBpkJEonVeN3xOvEEYJCZfMBqZRrRW0WUm9stJcUyYgQAELFVRFrkAN486hKlXonJ9n2ZK4v2hO5bcwxRdkRijs8gFQXqEuxRj-L9_2XYW1piADLLYoG2yj33K9EdBG9Iiyjk7vZkZDQcXn9MX8cAoVF4OYt7gN0G3EFuA_-lrFD686yEIzNPwt2264KBRu-bfRTRJSzfOL_WcXc3pEA4kPWTZWqWI_RpbAGCF9AjGBSEvzjhtggt2suEk',
                trangThai: 'Áp dụng'
            },
            {
                id: 3,
                ma: 'DC-VIN-03',
                ten: 'Vườn Hồng Paris Lãng Mạn',
                phanLoai: 'Cổ điển (Vintage Romance)',
                gia: 55000000,
                moTa: 'Tone màu hồng pastel kết hợp hoa hồng Ohara, chân nến đồng cổ điển phong cách Pháp cùng thảm hoa trải dài tạo không gian thơ mộng.',
                spec1: 'Photobooth • Bàn tiếp đón',
                spec2: '4.5 giờ trước ca tiệc',
                hinhAnh: 'https://lh3.googleusercontent.com/aida/AEtjO1UaS126VuKDx20PSjqyYgp_YMXJLOTkzCWFAVqO66ihF4jd-TqbYnPmOI75AiSe5S06gvii9zplBc9L3M_4zoIh3RBitPCdp_1Zb6jQppBET7qdHZ_n_qN7yM2Crpx_urD3lR0J85I8i5iWQsxLA65_Xur241NPCUQXUY5lgHSGMfepO8be8iCeMCuNAWapNq0WkdsqtXZups6vgDed6-ThJBaN0CrjroRXnN7h9GBTzyozf3FmY67hFgem',
                trangThai: 'Áp dụng'
            },
            {
                id: 4,
                ma: 'DC-CEL-04',
                ten: 'Bản Giao Hưởng Ánh Sao',
                phanLoai: 'Dạ khúc Ánh sao (Celestial)',
                gia: 82000000,
                moTa: 'Màn trần sao công nghệ sợi quang học vô cực, phối hợp hiệu ứng ánh sáng kinetic chuyển động cùng cụm hoa trắng pha lê phản quang hiện đại.',
                spec1: 'Trần sao vòm • Đèn Kinetic',
                spec2: '5 giờ trước ca tiệc',
                hinhAnh: '',
                isCelestialGraphic: true,
                trangThai: 'Áp dụng'
            },
            {
                id: 5,
                ma: 'DC-RIV-05',
                ten: 'Dạ Yến Pha Lê Ven Sông',
                phanLoai: 'Sang trọng bờ sông (Riverside Luxe)',
                gia: 52000000,
                moTa: 'Ý tưởng thiết kế độc quyền tôn vinh tầm nhìn sông Bến Vân Đồn với các khối trụ mica trong suốt phản chiếu ánh đèn vàng champagne và pha lê Tiệp.',
                spec1: 'Trụ hoa Mica • Bàn tiệc VIP',
                spec2: '3.5 giờ trước ca tiệc',
                hinhAnh: 'https://lh3.googleusercontent.com/aida/AEtjO1UlG53VliMfYaOPGWYTFo5zD5pEzc698JZZ3xtxh3ZJzk9TH4CKxNGW6GRCm35bJ5pSfaMfASYwgstAXnJfCb4pb8Ri73BetKzuyD9A1ZBVRo0tyUm1tOg7LoWxE_EwngYzmPNjYi7IYvP3Zxc0rUiuzJwCq8CIR3R327AKzJ6fz-C8v6WzJjdkOdsvUKVODnM21hg2IkUq4BQYQeXrPz_QG7aMOPFALohu5Lp2kFCu2PsDU5TivtSPHqU',
                trangThai: 'Áp dụng'
            },
            {
                id: 6,
                ma: 'DC-ARC-06',
                ten: 'Mùa Thu Vàng Venice',
                phanLoai: 'Cổ điển (Vintage Romance)',
                gia: 38000000,
                moTa: 'Gói trang trí hoa phong lá đỏ mùa thu Venice kèm hệ thống cổng chào gỗ mộc. Tạm dừng triển khai trong niên độ 2026 để nâng cấp mẫu.',
                spec1: 'Cổng gỗ mộc • Cành lá đỏ',
                spec2: 'Đang bảo quản kho vật tư',
                hinhAnh: '',
                trangThai: 'Ngừng áp dụng'
            }
        ];

        let serviceData = [
            {
                id: 101,
                ma: 'DV-SND-01',
                ten: 'Gói Âm Thanh Hòa Nhạc Line Array JBL',
                phanLoai: 'Âm thanh sân khấu',
                gia: 14500000,
                moTa: 'Hệ thống loa Line Array 8 module, sub kép công suất lớn, mixer Yamaha 32 kênh, 4 mic Shure không dây chống hú rít.',
                spec1: '2 kỹ thuật viên âm thanh',
                spec2: 'Setup trước ca 2.5h',
                hinhAnh: '',
                trangThai: 'Áp dụng'
            },
            {
                id: 102,
                ma: 'DV-LGT-02',
                ten: 'Gói Ánh Sáng Lễ Đường & Beam 380W',
                phanLoai: 'Ánh sáng sân khấu',
                gia: 12000000,
                moTa: 'Gồm 12 đèn Moving Head Beam 380W sắc nét, 16 đèn Par LED 54 bóng nhuộm màu, 2 máy tạo khói lạnh Haze cao cấp.',
                spec1: '1 kỹ sư lập trình DMX',
                spec2: 'Setup trước ca 3.0h',
                hinhAnh: '',
                trangThai: 'Áp dụng'
            },
            {
                id: 103,
                ma: 'DV-MC-01',
                ten: 'MC Nghi Thức & Khai Tiệc Song Ngữ (Anh - Việt)',
                phanLoai: 'MC Dẫn chương trình',
                gia: 6500000,
                moTa: 'MC chuyên nghiệp trên 5 năm kinh nghiệm tiệc cưới cao cấp, phong thái trang nhã, kịch bản may đo riêng theo câu chuyện CD-CR.',
                spec1: 'Trang phục Dạ hội / Vest Tuxedo',
                spec2: 'Có mặt trước ca 1.5h duyệt kịch bản',
                hinhAnh: '',
                trangThai: 'Áp dụng'
            },
            {
                id: 104,
                ma: 'DV-BND-03',
                ten: 'Ban Nhạc Tứ Tấu Dây Cổ Điển (String Quartet)',
                phanLoai: 'Ban nhạc & Hòa tấu',
                gia: 8500000,
                moTa: 'Gồm 2 Violin, 1 Viola, 1 Cello biểu diễn các bản tình ca cổ điển và pop cover lãng mạn suốt 60 phút đón khách tại Foyer.',
                spec1: '4 nhạc công Nhạc viện TP.HCM',
                spec2: 'Tổng thời lượng 90 phút',
                hinhAnh: '',
                trangThai: 'Áp dụng'
            },
            {
                id: 105,
                ma: 'DV-LED-01',
                ten: 'Màn Hình LED Sân Khấu Trung Tâm P2.5 (30m²)',
                phanLoai: 'Màn hình LED & Trình chiếu',
                gia: 18000000,
                moTa: 'Mật độ điểm ảnh siêu mịn P2.5, kích thước 7.5m x 4.0m, quét 3840Hz, xử lý tín hiệu trực tiếp từ camera live stream.',
                spec1: 'Bộ vi xử lý NovaStar cao cấp',
                spec2: '1 VJ trực visual sân khấu',
                hinhAnh: '',
                trangThai: 'Áp dụng'
            },
            {
                id: 106,
                ma: 'DV-BND-05',
                ten: 'Dàn Nhạc Giao Hưởng Thính Phòng Mini (12 Nhạc Công)',
                phanLoai: 'Ban nhạc & Hòa tấu',
                gia: 26000000,
                moTa: 'Biểu diễn trọn gói nghi thức thính phòng hoàng gia. Tạm ngưng để thẩm định lại danh mục nhân sự và phối khí 2026.',
                spec1: 'Dàn nhạc 12 nghệ sĩ chuyên nghiệp',
                spec2: 'Tạm dừng tiếp nhận hợp đồng mới',
                hinhAnh: '',
                trangThai: 'Ngừng áp dụng'
            }
        ];

        document.addEventListener('DOMContentLoaded', () => {
            switchMainTab('decor');

            // Bắt sự kiện click ngoài Dropdown Thông Báo để đóng tự động
            document.addEventListener('click', function(event) {
                const wrapper = document.getElementById('notification-wrapper');
                const dropdown = document.getElementById('notification-dropdown');
                if (wrapper && !wrapper.contains(event.target)) {
                    dropdown.classList.add('hidden');
                }
            });
        });

        // 2. TÍNH NĂNG CHUÔNG THÔNG BÁO
        function toggleNotificationDropdown() {
            const dropdown = document.getElementById('notification-dropdown');
            dropdown.classList.toggle('hidden');
        }

        function markAllNotificationsRead() {
            const badge = document.getElementById('notif-badge');
            if (badge) badge.classList.add('hidden');
            alert('Tất cả thông báo đã được đánh dấu là Đã đọc.');
        }

        // 3. CHUYỂN ĐỔI TAB HOÀN TOÀN MƯỢT MÀ VÀ ĐỒNG BỘ
        function switchMainTab(tab) {
            currentTab = tab;
            const btnDecor = document.getElementById('tab-btn-decor');
            const btnService = document.getElementById('tab-btn-service');
            const containerDecor = document.getElementById('container-decor-tab');
            const containerService = document.getElementById('container-service-tab');
            const breadcrumbTab = document.getElementById('breadcrumb-current-tab');
            const headerTitle = document.getElementById('header-page-title');
            const btnCreateLabel = document.getElementById('btn-create-label');
            const inputSearch = document.getElementById('input-search-keyword');
            const labelCat = document.getElementById('label-filter-category');
            const iconFilterCat = document.getElementById('icon-filter-cat');
            const metricTitleLabel = document.getElementById('metric-title-label');

            if (tab === 'decor') {
                btnDecor.className = 'py-4 font-bold text-sm border-b-2 border-[#B89758] text-slate-900 flex items-center gap-2 relative focus-ring transition-colors';
                btnDecor.innerHTML = `<i class="fa-solid fa-wand-magic-sparkles text-[#B89758]"></i><span>Gói Decor Tiệc Cưới</span><span class="bg-[#B89758]/15 text-[#8C6D32] text-[11px] font-bold px-2 py-0.5 rounded-full ml-1 border border-[#B89758]/30">Đang chọn</span>`;

                btnService.className = 'py-4 font-medium text-sm text-slate-500 hover:text-slate-800 flex items-center gap-2 transition-colors border-b-2 border-transparent focus-ring';
                btnService.innerHTML = `<i class="fa-solid fa-people-arrows text-slate-400"></i><span>Dịch Vụ Đi Kèm (Âm thanh, MC, Ban nhạc, LED)</span><span class="text-[11px] bg-slate-100 text-slate-600 px-2 py-0.5 rounded-full border border-slate-200">${serviceData.length} dịch vụ</span>`;

                containerDecor.classList.remove('hidden');
                containerService.classList.add('hidden');

                breadcrumbTab.innerText = 'Gói Decor Tiệc Cưới';
                headerTitle.innerText = 'Quản Trị Danh Mục Gói Decor Tiệc Cưới';
                btnCreateLabel.innerText = 'Thêm gói Decor';
                inputSearch.placeholder = 'Tìm mã gói, tên chủ đề decor...';
                labelCat.innerText = 'Phong cách:';
                iconFilterCat.className = 'fa-solid fa-palette text-slate-500 mr-2 text-xs';
                metricTitleLabel.innerText = 'Danh mục Gói Decor đang quản lý:';
            } else {
                btnService.className = 'py-4 font-bold text-sm border-b-2 border-[#B89758] text-slate-900 flex items-center gap-2 relative focus-ring transition-colors';
                btnService.innerHTML = `<i class="fa-solid fa-people-arrows text-[#B89758]"></i><span>Dịch Vụ Đi Kèm (Âm thanh, MC, Ban nhạc, LED)</span><span class="bg-[#B89758]/15 text-[#8C6D32] text-[11px] font-bold px-2.5 py-0.5 rounded-full ml-1 border border-[#B89758]/30">Đang chọn</span>`;

                btnDecor.className = 'py-4 font-medium text-sm text-slate-500 hover:text-slate-800 flex items-center gap-2 transition-colors border-b-2 border-transparent focus-ring';
                btnDecor.innerHTML = `<i class="fa-solid fa-wand-magic-sparkles text-slate-400"></i><span>Gói Decor Tiệc Cưới</span><span class="bg-slate-100 text-slate-600 text-[11px] font-semibold px-2 py-0.5 rounded-full ml-1">${decorData.length} gói</span>`;

                containerService.classList.remove('hidden');
                containerDecor.classList.add('hidden');

                breadcrumbTab.innerText = 'Dịch vụ đi kèm';
                headerTitle.innerText = 'Quản Trị Danh Mục Dịch Vụ Đi Kèm';
                btnCreateLabel.innerText = 'Thêm dịch vụ mới';
                inputSearch.placeholder = 'Tìm mã DV, tên dịch vụ, nhân sự...';
                labelCat.innerText = 'Loại dịch vụ:';
                iconFilterCat.className = 'fa-solid fa-layer-group text-slate-500 mr-2 text-xs';
                metricTitleLabel.innerText = 'Danh mục Dịch vụ đang quản lý:';
            }

            populateCategoryFilter();
            applyFilters();
        }

        // 4. ĐỔ OPTIONS PHÂN LOẠI / PHONG CÁCH VÀO DROPDOWN
        function populateCategoryFilter() {
            const select = document.getElementById('filter-category');
            select.innerHTML = '<option value="">' + (currentTab === 'decor' ? 'Tất cả phong cách' : 'Tất cả phân loại') + '</option>';
            const currentList = currentTab === 'decor' ? decorData : serviceData;
            const categories = [...new Set(currentList.map(item => item.phanLoai))];
            categories.forEach(cat => {
                const opt = document.createElement('option');
                opt.value = cat;
                opt.innerText = cat;
                select.appendChild(opt);
            });
        }

        // 5. LỌC & TÌM KIẾM DỮ LIỆU
        function applyFilters() {
            const keyword = document.getElementById('input-search-keyword').value.toLowerCase().trim();
            const category = document.getElementById('filter-category').value;
            const status = document.getElementById('filter-status').value;

            const sourceList = currentTab === 'decor' ? decorData : serviceData;
            const filtered = sourceList.filter(item => {
                const matchKw = !keyword || item.ma.toLowerCase().includes(keyword) || item.ten.toLowerCase().includes(keyword) || item.moTa.toLowerCase().includes(keyword);
                const matchCat = !category || item.phanLoai === category;
                const matchStatus = !status || item.trangThai === status;
                return matchKw && matchCat && matchStatus;
            });

            if (currentTab === 'decor') {
                renderDecorCards(filtered);
            } else {
                renderServiceTable(filtered);
            }
            updateMetrics(filtered, sourceList);
        }

        function resetFilters() {
            document.getElementById('input-search-keyword').value = '';
            document.getElementById('filter-category').value = '';
            document.getElementById('filter-status').value = '';
            applyFilters();
        }

        // 6. CẬP NHẬT CHỈ SỐ METRICS
        function updateMetrics(filteredList, totalList) {
            const activeCount = totalList.filter(x => x.trangThai === 'Áp dụng').length;
            const inactiveCount = totalList.filter(x => x.trangThai === 'Ngừng áp dụng').length;

            document.getElementById('metric-showing').innerText = `Hiển thị ${filteredList.length} ${currentTab === 'decor' ? 'gói' : 'dịch vụ'}`;
            document.getElementById('metric-active').innerHTML = `<span class="w-2 h-2 rounded-full bg-emerald-500"></span> ${activeCount} Đang áp dụng`;
            document.getElementById('metric-inactive').innerHTML = `<span class="w-2 h-2 rounded-full bg-slate-400"></span> ${inactiveCount} Ngừng áp dụng`;
            document.getElementById('pagination-summary').innerHTML = `Hiển thị <strong>1 – ${filteredList.length}</strong> trong tổng số <strong>${totalList.length}</strong> ${currentTab === 'decor' ? 'gói Decor' : 'dịch vụ'}`;
            document.getElementById('badge-system-status').innerText = `Hệ thống sẵn sàng • ${activeCount}/${totalList.length} Áp dụng`;
        }

        // 7. RENDER GIAO DIỆN TAB 1: CARD GRID
        function renderDecorCards(list) {
            const container = document.getElementById('container-decor-tab');
            container.innerHTML = '';

            if (list.length === 0) {
                container.innerHTML = `<div class="col-span-full bg-white rounded-xl p-12 text-center text-slate-400 border border-slate-200"><i class="fa-regular fa-folder-open text-3xl mb-2 text-slate-300"></i><p>Không tìm thấy gói Decor nào phù hợp với bộ lọc hiện tại.</p></div>`;
                return;
            }

            list.forEach(item => {
                const isInactive = item.trangThai === 'Ngừng áp dụng';
                let mediaBlock = '';

                if (item.isCelestialGraphic) {
                    mediaBlock = `
                        <div class="relative w-full aspect-[16/10] overflow-hidden bg-[#101721] flex items-center justify-center">
                            <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_40%,#1e293b,transparent_70%)]"></div>
                            <div class="relative z-10 text-center px-4">
                                <div class="w-12 h-12 mx-auto mb-2 rounded-full bg-[#B89758]/15 border border-[#B89758]/40 flex items-center justify-center text-[#B89758]">
                                    <i class="fa-solid fa-star text-base"></i>
                                </div>
                                <p class="font-serif text-sm font-semibold text-white tracking-widest uppercase">Celestial Symphony</p>
                                <p class="text-[10px] text-slate-400 mt-0.5">Trần sao quang sợi &amp; Màn sao LED Kinetic</p>
                            </div>
                            <div class="absolute top-3 inset-x-3 flex items-center justify-between">
                                <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-md text-[11px] font-bold bg-emerald-950/80 text-emerald-300 border border-emerald-500/30 backdrop-blur-sm shadow-sm">
                                    <span class="w-1.5 h-1.5 rounded-full bg-emerald-400"></span> Áp dụng
                                </span>
                                <span class="px-2 py-0.5 rounded-md text-[10.5px] font-mono font-semibold bg-black/60 text-slate-200 backdrop-blur-sm border border-white/20">${item.ma}</span>
                            </div>
                            <div class="absolute bottom-3 left-3">
                                <span class="px-2.5 py-1 rounded-md text-[11px] font-medium bg-[#141E28]/80 text-[#E8D8B6] backdrop-blur-sm border border-[#B89758]/40 flex items-center gap-1">
                                    <i class="fa-solid fa-wand-magic text-[10px] text-indigo-300"></i> ${item.phanLoai}
                                </span>
                            </div>
                        </div>`;
                } else if (item.hinhAnh) {
                    mediaBlock = `
                        <div class="relative w-full aspect-[16/10] overflow-hidden bg-slate-100">
                            <img src="${item.hinhAnh}" alt="${item.ten}" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500">
                            <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-black/20 pointer-events-none"></div>
                            <div class="absolute top-3 inset-x-3 flex items-center justify-between">
                                <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-md text-[11px] font-bold ${isInactive ? 'bg-slate-800/85 text-slate-200 border border-slate-600/50' : 'bg-emerald-950/80 text-emerald-300 border border-emerald-500/30'} backdrop-blur-sm shadow-sm">
                                    <span class="w-1.5 h-1.5 rounded-full ${isInactive ? 'bg-slate-400' : 'bg-emerald-400'}"></span> ${item.trangThai}
                                </span>
                                <span class="px-2 py-0.5 rounded-md text-[10.5px] font-mono font-semibold bg-black/60 text-slate-200 backdrop-blur-sm border border-white/20">${item.ma}</span>
                            </div>
                            <div class="absolute bottom-3 left-3">
                                <span class="px-2.5 py-1 rounded-md text-[11px] font-medium bg-[#141E28]/80 text-[#E8D8B6] backdrop-blur-sm border border-[#B89758]/40 flex items-center gap-1">
                                    <i class="fa-solid fa-palette text-[10px] text-[#B89758]"></i> ${item.phanLoai}
                                </span>
                            </div>
                        </div>`;
                } else {
                    mediaBlock = `
                        <div class="relative w-full aspect-[16/10] bg-slate-100 border-b border-slate-200 flex flex-col items-center justify-center p-6 text-center group">
                            <div class="w-12 h-12 rounded-xl bg-slate-200 flex items-center justify-center text-slate-400 mb-2">
                                <i class="fa-regular fa-image text-xl"></i>
                            </div>
                            <p class="text-xs font-semibold text-slate-600">Hình ảnh tư liệu đang lưu trữ</p>
                            <p class="text-[10.5px] text-slate-400 mt-0.5">Bản vẽ phối cảnh 3D chưa cập nhật</p>
                            <div class="absolute top-3 inset-x-3 flex items-center justify-between">
                                <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-md text-[11px] font-bold bg-slate-800/85 text-slate-200 border border-slate-600/50 backdrop-blur-sm shadow-sm">
                                    <span class="w-1.5 h-1.5 rounded-full bg-slate-400"></span> ${item.trangThai}
                                </span>
                                <span class="px-2 py-0.5 rounded-md text-[10.5px] font-mono font-semibold bg-slate-700/60 text-slate-200 backdrop-blur-sm border border-slate-500/30">${item.ma}</span>
                            </div>
                            <div class="absolute bottom-3 left-3">
                                <span class="px-2.5 py-1 rounded-md text-[11px] font-medium bg-white/90 text-slate-700 shadow-sm border border-slate-200 flex items-center gap-1">
                                    <i class="fa-solid fa-landmark text-[10px] text-slate-500"></i> ${item.phanLoai}
                                </span>
                            </div>
                        </div>`;
                }

                const card = document.createElement('div');
                card.className = `bg-white rounded-xl border border-slate-200 shadow-sm hover:border-[#B89758]/50 hover:shadow-md transition-all flex flex-col overflow-hidden group ${isInactive ? 'opacity-90' : ''}`;
                card.innerHTML = `
                    ${mediaBlock}
                    <div class="p-5 flex-1 flex flex-col justify-between space-y-4">
                        <div>
                            <h3 class="font-serif font-bold text-lg text-slate-900 group-hover:text-[#8C6D32] transition-colors leading-snug">
                                ${item.ten}
                            </h3>
                            <p class="text-xs text-slate-600 mt-2 leading-relaxed line-clamp-2">${item.moTa}</p>

                            <div class="mt-3 pt-3 border-t border-dashed border-slate-200 grid grid-cols-2 gap-2 text-[11px] text-slate-500">
                                <div>
                                    <span class="block text-[10px] uppercase font-semibold tracking-wider text-slate-400">Hạng mục chính:</span>
                                    <span class="text-slate-700 font-medium truncate block">${item.spec1 || 'Toàn bộ sảnh tiệc'}</span>
                                </div>
                                <div>
                                    <span class="block text-[10px] uppercase font-semibold tracking-wider text-slate-400">Thời gian set-up:</span>
                                    <span class="text-slate-700 font-medium block">${item.spec2 || '4.0 giờ trước ca'}</span>
                                </div>
                            </div>
                        </div>

                        <div class="pt-3 border-t border-slate-100 flex items-baseline justify-between">
                            <span class="text-xs text-slate-500 font-medium">${isInactive ? 'Đơn giá lưu trữ:' : 'Đơn giá niêm yết:'}</span>
                            <div class="text-right">
                                <span class="text-lg font-bold text-slate-900 font-serif tracking-tight">${Number(item.gia).toLocaleString('vi-VN')}</span>
                                <span class="text-xs font-semibold text-slate-500 ml-0.5">VNĐ</span>
                            </div>
                        </div>

                        <div class="pt-1 grid grid-cols-3 gap-2">
                            <button onclick="viewDetail(${item.id})" class="min-h-[40px] px-2 py-2 rounded-lg bg-slate-50 hover:bg-slate-100 text-slate-700 text-xs font-semibold border border-slate-200 transition-colors flex items-center justify-center gap-1.5 focus-ring" title="Xem chi tiết">
                                <i class="fa-regular fa-eye text-slate-500"></i><span>Chi tiết</span>
                            </button>
                            <button onclick="editDecorItem(${item.id})" class="min-h-[40px] px-2 py-2 rounded-lg bg-slate-50 hover:bg-[#B89758]/10 text-slate-800 hover:text-[#8C6D32] text-xs font-semibold border border-slate-200 hover:border-[#B89758]/50 transition-colors flex items-center justify-center gap-1.5 focus-ring" title="Chỉnh sửa">
                                <i class="fa-regular fa-pen-to-square text-[#B89758]"></i><span>Sửa</span>
                            </button>
                            <button onclick="toggleStatus(${item.id})" class="min-h-[40px] px-2 py-2 rounded-lg ${isInactive ? 'bg-emerald-50 hover:bg-emerald-100 text-emerald-800 border-emerald-200' : 'bg-slate-50 hover:bg-amber-50 text-slate-700 hover:text-amber-800 border-slate-200'} text-xs font-semibold border transition-colors flex items-center justify-center gap-1.5 focus-ring" title="Thay đổi trạng thái">
                                <i class="fa-solid ${isInactive ? 'fa-rotate-left text-emerald-600' : 'fa-lock text-slate-400'}"></i>
                                <span>${isInactive ? 'Mở lại' : 'Khóa'}</span>
                            </button>
                        </div>
                    </div>
                `;
                container.appendChild(card);
            });
        }

        // 8. RENDER GIAO DIỆN TAB 2: OPERATIONAL TABLE
        function renderServiceTable(list) {
            const tbody = document.getElementById('service-table-tbody');
            tbody.innerHTML = '';

            if (list.length === 0) {
                tbody.innerHTML = `<tr><td colspan="7" class="text-center py-10 text-slate-400 italic">Không tìm thấy dịch vụ nào phù hợp với bộ lọc.</td></tr>`;
                return;
            }

            list.forEach(item => {
                const isInactive = item.trangThai === 'Ngừng áp dụng';
                const tr = document.createElement('tr');
                tr.className = `custom-row-hover transition-colors ${isInactive ? 'bg-slate-50/40' : ''}`;

                const badgeStatus = !isInactive
                    ? `<span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-[11px] font-bold bg-emerald-50 text-emerald-700 border border-emerald-200"><span class="w-1.5 h-1.5 rounded-full bg-emerald-500"></span> Đang áp dụng</span>`
                    : `<span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-[11px] font-bold bg-slate-100 text-slate-600 border border-slate-300"><span class="w-1.5 h-1.5 rounded-full bg-slate-400"></span> Tạm ngưng</span>`;

                const lockBtn = !isInactive
                    ? `<button onclick="toggleStatus(${item.id})" class="h-[36px] px-2.5 rounded-lg bg-amber-50 hover:bg-amber-100 text-amber-800 text-xs font-semibold flex items-center gap-1 border border-amber-200 transition-colors" title="Khóa tạm ngưng"><i class="fa-solid fa-ban text-[11px]"></i><span>Khóa</span></button>`
                    : `<button onclick="toggleStatus(${item.id})" class="h-[36px] px-2.5 rounded-lg bg-emerald-50 hover:bg-emerald-100 text-emerald-800 text-xs font-semibold flex items-center gap-1 border border-emerald-200 transition-colors" title="Kích hoạt lại"><i class="fa-solid fa-rotate-left text-[11px]"></i><span>Mở lại</span></button>`;

                const imageThumbnail = item.hinhAnh
                    ? `<div class="w-32 h-20 rounded-lg overflow-hidden border border-gray-200 relative bg-slate-900 group"><img src="${item.hinhAnh}" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300" /><span class="absolute bottom-1 right-1 bg-black/70 text-white text-[9px] px-1.5 py-0.5 rounded">16:10</span></div>`
                    : `<div class="w-32 h-20 rounded-lg border border-dashed border-slate-300 bg-[#F5F3EF] flex flex-col items-center justify-center text-slate-400 p-2 text-center"><i class="fa-regular fa-image text-lg mb-1"></i><span class="text-[9px] text-slate-500 font-medium">Chưa có ảnh 16:10</span><span class="text-[8px] text-slate-400">Đang cập nhật</span></div>`;

                tr.innerHTML = `
                    <td class="py-4 px-4 text-center">
                        <span class="font-mono text-[11px] font-bold px-2 py-1 rounded bg-slate-100 text-slate-800 border border-slate-200">${item.ma}</span>
                    </td>
                    <td class="py-4 px-4">${imageThumbnail}</td>
                    <td class="py-4 px-5 align-middle">
                        <div class="space-y-1">
                            <h4 class="font-serif font-bold text-sm text-slate-900">${item.ten}</h4>
                            <p class="text-slate-600 text-xs line-clamp-2 leading-relaxed">${item.moTa}</p>
                            <div class="text-[11px] text-slate-400 flex items-center gap-3 pt-0.5">
                                <span><i class="fa-solid fa-user-gear text-[10px] text-[#B89758] mr-1"></i>${item.spec1 || '2 kỹ thuật viên'}</span>
                                <span><i class="fa-regular fa-clock text-[10px] text-[#B89758] mr-1"></i>${item.spec2 || 'Setup trước ca 2h'}</span>
                            </div>
                        </div>
                    </td>
                    <td class="py-4 px-4">
                        <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-md bg-amber-50/80 text-amber-900 border border-[#B89758]/30 text-xs font-medium">
                            <i class="fa-solid fa-tag text-[10px] text-[#B89758]"></i> ${item.phanLoai}
                        </span>
                    </td>
                    <td class="py-4 px-5 text-right">
                        <div class="font-serif font-bold text-sm text-slate-900">${Number(item.gia).toLocaleString('vi-VN')} <span class="text-xs font-sans text-slate-500 font-normal">VNĐ</span></div>
                        <span class="text-[10px] text-slate-400 block mt-0.5">/suất tiệc</span>
                    </td>
                    <td class="py-4 px-4 text-center">${badgeStatus}</td>
                    <td class="py-4 px-4 text-center">
                        <div class="flex items-center justify-center gap-1.5">
                            <button onclick="viewDetail(${item.id})" class="h-[36px] px-2.5 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-700 text-xs font-semibold flex items-center gap-1 border border-slate-200 transition-colors" title="Xem chi tiết">
                                <i class="fa-regular fa-eye text-slate-500"></i><span>Xem</span>
                            </button>
                            <button onclick="editDecorItem(${item.id})" class="h-[36px] px-2.5 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-700 text-xs font-semibold flex items-center gap-1 border border-slate-200 transition-colors" title="Chỉnh sửa">
                                <i class="fa-regular fa-pen-to-square text-[#B89758]"></i><span>Sửa</span>
                            </button>
                            ${lockBtn}
                        </div>
                    </td>
                `;
                tbody.appendChild(tr);
            });
        }

        // 9. KHÓA / MỞ TRẠNG THÁI RECORD
        function toggleStatus(id) {
            const list = currentTab === 'decor' ? decorData : serviceData;
            const target = list.find(x => x.id === id);
            if (target) {
                target.trangThai = target.trangThai === 'Áp dụng' ? 'Ngừng áp dụng' : 'Áp dụng';
                applyFilters();
            }
        }

        // 10. MODAL XEM CHI TIẾT
        function viewDetail(id) {
            const list = currentTab === 'decor' ? decorData : serviceData;
            const item = list.find(x => x.id === id);
            if (!item) return;

            document.getElementById('detail-title').innerText = currentTab === 'decor' ? `Chi Tiết Gói Decor: ${item.ma}` : `Chi Tiết Dịch Vụ: ${item.ma}`;
            document.getElementById('detail-content').innerHTML = `
                <div class="p-3 bg-slate-50 rounded-lg border border-slate-200 flex justify-between items-center">
                    <div>
                        <h4 class="font-bold text-sm text-slate-800">${item.ten}</h4>
                        <span class="text-[11px] text-[#B89758] font-semibold">${item.phanLoai}</span>
                    </div>
                    <div class="text-right font-serif font-bold text-base text-slate-900">
                        ${Number(item.gia).toLocaleString('vi-VN')} VNĐ
                    </div>
                </div>
                <div>
                    <span class="font-bold text-slate-700 block mb-1">Mô tả cấu hình:</span>
                    <p class="text-slate-600 leading-relaxed bg-white p-3 rounded-lg border border-slate-100">${item.moTa}</p>
                </div>
                <div class="grid grid-cols-2 gap-3">
                    <div class="p-2.5 bg-slate-50 rounded-lg border border-slate-200">
                        <span class="text-[10px] uppercase font-bold text-slate-400 block">${currentTab === 'decor' ? 'Hạng mục chính' : 'Nhân sự vận hành'}</span>
                        <span class="font-semibold text-slate-800">${item.spec1 || 'Tiêu chuẩn'}</span>
                    </div>
                    <div class="p-2.5 bg-slate-50 rounded-lg border border-slate-200">
                        <span class="text-[10px] uppercase font-bold text-slate-400 block">${currentTab === 'decor' ? 'Thời gian set-up' : 'Thời gian phục vụ'}</span>
                        <span class="font-semibold text-slate-800">${item.spec2 || 'Theo ca tiệc'}</span>
                    </div>
                </div>
                <div class="flex items-center justify-between text-[11px] text-slate-500 pt-2">
                    <span>Tình trạng niêm yết: <strong class="${item.trangThai === 'Áp dụng' ? 'text-emerald-600' : 'text-slate-500'} font-bold">${item.trangThai}</strong></span>
                    <span>Mã quy chuẩn: <strong>MD-RP-2026</strong></span>
                </div>
            `;
            document.getElementById('detailModal').classList.remove('hidden');
        }

        function closeDetailModal() {
            document.getElementById('detailModal').classList.add('hidden');
        }

        // 11. XỬ LÝ MODAL THÊM MỚI GÓI DECOR
        function openCreateModal() {
            document.getElementById('decorForm').reset();
            document.getElementById('decor-form-id').value = '0';
            document.getElementById('decor-modal-title').innerText = currentTab === 'decor' ? 'Thêm gói Decor mới' : 'Thêm Dịch Vụ Mới';

            // Tạo mã tự động tăng
            if (currentTab === 'decor') {
                document.getElementById('decor-code').value = 'DC-ROY-0' + (decorData.length + 1);
                document.getElementById('decor-name').value = 'Hoàng Kim Dạ Yến';
                document.getElementById('decor-price').value = '72000000';
                document.getElementById('decor-setup-time').value = '4.0 giờ trước ca tiệc';
                document.getElementById('decor-desc').value = 'Gói trang trí Hoàng Kim Dạ Yến sử dụng gam màu vàng Champagne kết hợp trắng ngà thanh thoát, hoa lan hồ điệp tươi kết hợp hoa hồng nhập khẩu. Điểm nhấn là vòm cổng pha lê cao cấp và hệ đèn chandelier lộng lẫy kiến tạo không gian đại sảnh đẳng cấp quý tộc.';
            } else {
                document.getElementById('decor-code').value = 'DV-SND-0' + (serviceData.length + 1);
                document.getElementById('decor-name').value = '';
                document.getElementById('decor-price').value = '10000000';
                document.getElementById('decor-setup-time').value = '2.0 giờ trước ca tiệc';
                document.getElementById('decor-desc').value = '';
            }

            document.getElementById('radio-decor-active').checked = true;
            document.getElementById('decor-image-preview-card').classList.remove('hidden');
            document.getElementById('decorModal').classList.remove('hidden');
        }

        function editDecorItem(id) {
            const list = currentTab === 'decor' ? decorData : serviceData;
            const item = list.find(x => x.id === id);
            if (!item) return;

            document.getElementById('decor-form-id').value = item.id;
            document.getElementById('decor-modal-title').innerText = currentTab === 'decor' ? 'Chỉnh sửa gói Decor' : 'Chỉnh sửa Dịch vụ';
            document.getElementById('decor-code').value = item.ma;
            document.getElementById('decor-name').value = item.ten;
            document.getElementById('decor-style').value = item.phanLoai;
            document.getElementById('decor-price').value = item.gia;
            document.getElementById('decor-setup-time').value = item.spec2 || '4.0 giờ trước ca tiệc';
            document.getElementById('decor-desc').value = item.moTa;

            if (item.trangThai === 'Áp dụng') {
                document.getElementById('radio-decor-active').checked = true;
            } else {
                document.getElementById('radio-decor-draft').checked = true;
            }

            if (item.hinhAnh) {
                document.getElementById('decor-preview-img').src = item.hinhAnh;
                document.getElementById('decor-preview-filename').innerText = item.ma + '-cover-image.jpg';
                document.getElementById('decor-image-preview-card').classList.remove('hidden');
            }

            document.getElementById('decorModal').classList.remove('hidden');
        }

        function closeDecorModal() {
            document.getElementById('decorModal').classList.add('hidden');
        }

        // 12. XỬ LÝ UPLOAD & PREVIEW ẢNH
        function handleImageSelected(event) {
            const file = event.target.files[0];
            if (file) {
                if (file.size > 5 * 1024 * 1024) {
                    alert('Tệp quá lớn! Vui lòng chọn ảnh có dung lượng dưới 5 MB.');
                    return;
                }
                const reader = new FileReader();
                reader.onload = function(e) {
                    document.getElementById('decor-preview-img').src = e.target.result;
                    document.getElementById('decor-preview-filename').innerText = file.name;
                    document.getElementById('decor-image-preview-card').classList.remove('hidden');
                };
                reader.readAsDataURL(file);
            }
        }

        function removeImagePreview() {
            document.getElementById('decor-file-input').value = '';
            document.getElementById('decor-image-preview-card').classList.add('hidden');
        }

        // 13. SUBMIT FORM
        function handleDecorFormSubmit(e) {
            e.preventDefault();
            const id = parseInt(document.getElementById('decor-form-id').value);
            const list = currentTab === 'decor' ? decorData : serviceData;

            const isCheckedActive = document.getElementById('radio-decor-active').checked;
            const previewImgSrc = document.getElementById('decor-preview-img').src;
            const isCardHidden = document.getElementById('decor-image-preview-card').classList.contains('hidden');

            const record = {
                id: id === 0 ? Date.now() : id,
                ma: document.getElementById('decor-code').value.trim(),
                ten: document.getElementById('decor-name').value.trim(),
                phanLoai: document.getElementById('decor-style').value.trim(),
                gia: parseFloat(document.getElementById('decor-price').value),
                spec1: 'Backdrop • Lối đi • Bàn VIP',
                spec2: document.getElementById('decor-setup-time').value.trim(),
                moTa: document.getElementById('decor-desc').value.trim(),
                hinhAnh: isCardHidden ? '' : previewImgSrc,
                trangThai: isCheckedActive ? 'Áp dụng' : 'Ngừng áp dụng'
            };

            if (id === 0) {
                list.unshift(record);
            } else {
                const index = list.findIndex(x => x.id === id);
                if (index !== -1) list[index] = record;
            }

            closeDecorModal();
            populateCategoryFilter();
            applyFilters();
        }

        // 14. XỬ LÝ XUẤT EXCEL THỰC TẾ BẰNG SHEETJS
        function exportDataExcel() {
            // Xác định dữ liệu dựa trên Tab đang chọn
            const list = currentTab === 'decor' ? decorData : serviceData;
            const fileName = currentTab === 'decor' ? 'Riverside_DanhMuc_GoiDecor.xlsx' : 'Riverside_DanhMuc_DichVu.xlsx';
            const sheetName = currentTab === 'decor' ? 'Goi_Decor' : 'Dich_Vu';

            // Chuyển đổi dữ liệu sang định dạng phù hợp với Excel (Tiếng Việt có dấu)
            const exportData = list.map((item, index) => ({
                'STT': index + 1,
                'Mã Quy Định': item.ma,
                'Tên Gói / Dịch Vụ': item.ten,
                'Phân Loại / Phong Cách': item.phanLoai,
                'Đơn Giá Niêm Yết (VNĐ)': item.gia,
                'Mô Tả Chi Tiết': item.moTa,
                'Hạng Mục Chính / Nhân Sự': item.spec1 || '',
                'Thời Gian Set-up / Phục Vụ': item.spec2 || '',
                'Trạng Thái': item.trangThai
            }));

            // Tạo Worksheet & Workbook
            const ws = XLSX.utils.json_to_sheet(exportData);
            const wb = XLSX.utils.book_new();
            XLSX.utils.book_append_sheet(wb, ws, sheetName);

            // Tự động căn chỉnh độ rộng cột cho đẹp
            const wscols = [
                {wch: 5},  // STT
                {wch: 15}, // Mã
                {wch: 35}, // Tên
                {wch: 25}, // Phân loại
                {wch: 20}, // Giá
                {wch: 60}, // Mô tả
                {wch: 30}, // Hạng mục
                {wch: 25}, // Thời gian
                {wch: 15}  // Trạng thái
            ];
            ws['!cols'] = wscols;

            // Kích hoạt tải xuống
            XLSX.writeFile(wb, fileName);
        }
    </script>
}